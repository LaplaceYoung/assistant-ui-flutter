# Element coverage

Generated by `dart run tool/sync_element_coverage.dart` — do not edit by hand.

| Element | Status | Wave | Flutter | Note |
|---|---|---|---|---|
| `activity-graph` | ported | — | `components/activity_graph.dart` | contribution calendar with the range total |
| `agent-card` | ported | — | `components/agent_card.dart` | skills, endpoint footer, connect / connected |
| `agent-handoff` | ported | — | `components/agent_handoff.dart` | from -> to pills, reason, carried-over list |
| `agent-plan` | ported | — | `components/agent_plan.dart` | progress bar, per-step check / spinner / dot |
| `agent-status` | ported | — | `components/agent_status.dart` | working/waiting/done/failed chip with elapsed and trailing control |
| `approval-card` | ported | — | `components/approval_card.dart` | request/running/done/denied states |
| `artifact-card` | ported | — | `components/artifact_card.dart` | live word count while generating, meta when settled, hover arrow |
| `assistant-modal` | ported | — | `components/assistant_modal.dart` | corner bubble, resizable panel with thread / list views, keyboard nudge and reset; the size is reported to the host instead of localStorage |
| `assistant-sidebar` | ported | — | `components/thread_list.dart` | rail with collapse-to-icon, drag-to-resize with min/max clamps, and the copilot side (rail on the right); below the mobile breakpoint it becomes a drawer |
| `attachment` | partial | — | `components/attachment.dart` | preview + progress + remove and the AttachmentAdapter contract are ported; a concrete storage backend stays with the host, as upstream leaves it to the app |
| `background-inbox` | ported | — | `components/background_inbox.dart` | ready vs in-flight counts, collect on tap |
| `canvas-split` | ported | — | `components/canvas_split.dart` | one widget instead of upstream seven pieces; md breakpoint stacks |
| `chart` | ported | — | `components/chart.dart` | area / line / bars variants over one x-scale; CustomPaint instead of SVG |
| `chat-panel` | ported | — | `components/chat_panel.dart` | compact panel with messages, typing row and composer |
| `checkpoint-history` | ported | — | `components/checkpoint_history.dart` | current mark, ahead rows dimmed, hover restore |
| `code-diff` | ported | — | `components/code_diff.dart` | unified diff with gutter marks, tints and the +/- counts |
| `code-runner` | ported | — | `components/code_runner.dart` | run control, duration, output panel; takes a highlighter override |
| `command-palette` | ported | — | `components/command_palette.dart` | filter, grouped order, arrow wrap, enter to run |
| `comparison-card` | ported | — | `components/comparison_card.dart` | option columns, pick mark, trait checks and dashes |
| `composer` | ported | — | `components/composer.dart` | input/send/cancel/attachments/voice/queue/context done; the model picker and the quote preview drop into the leading/footer slots, covered by a wiring test |
| `composer-trigger-popover` | ported | — | `primitives/composer_triggers.dart` | char trigger, caret token detection, keyboard nav, anchored panel |
| `computer-use` | ported | — | `components/computer_use.dart` | browser chrome, cursor trail, active step footer |
| `confidence-marker` | ported | — | `components/confidence_marker.dart` | underlines by confidence, basis pill while hovered |
| `connection-state` | ported | — | `components/connection_state.dart` | online renders nothing; dropped / reconnecting / resumed |
| `context-breakdown` | ported | — | `components/context_breakdown.dart` | segment bar, comma-formatted tokens, headroom row |
| `context-display` | ported | — | `components/composer_context.dart` | usage ring + optional token label from thread.contextUsage |
| `conversation-map` | ported | — | `components/conversation_map.dart` | rail of ticks per turn with previews, arrow-key stepping and the active mark |
| `conversation-search` | ported | — | `components/conversation_search.dart` | query field, hit counter, stepping, matched excerpt and the position rail |
| `cost-meter` | ported | — | `components/cost_meter.dart` | run vs session, per-model share bar and token counts |
| `data-table` | ported | — | `components/data_table.dart` | model / context / cost columns with the initial glyph |
| `day-separator` | ported | — | `components/day_separator.dart` | rules the day changes, times on hover |
| `diagram` | ported | — | `components/diagram.dart` | zoom in/out/reset/expand over a host figure |
| `directive-text` | ported | — | `components/composer_triggers.dart` | @[Name](id) rendered as inline chips |
| `document-reference` | ported | — | `components/document_reference.dart` | page anchors with quotes, active page highlighted |
| `draft-restore` | ported | — | `runtime/local_runtime.dart` | per-thread composer drafts survive thread switching and run start |
| `edit-message` | ported | — | `components/composer.dart` | edit in place, truncate and rerun, plus the discard notice |
| `elicitation-form` | ported | — | `components/elicitation_form.dart` | text/choice/toggle fields; adds selected + onFieldChanged over upstream display-only |
| `empty-state` | ported | — | `components/empty_state.dart` | greeting, prompt pills and the stand-in composer |
| `error-state` | ported | — | `components/error_state.dart` | red card with the retry, or the shimmering retrying line |
| `feedback-dialog` | ported | — | `components/feedback_dialog.dart` | reason chips, note field, send, then the live-region acknowledgement |
| `file` | partial | — | `components/parts.dart` | chip carries the mime icon set, a type label, the payload size when the part carries base64 bytes, and a download hook the host wires |
| `file-tree` | ported | — | `components/file_tree.dart` | folder / file rows with per-file diff counts and a total |
| `flow` | ported | — | `components/flow.dart` | root frame, rows / columns, dashed groups, box and decision nodes with tones, labelled arrows |
| `flow-canvas` | ported | — | `components/flow_canvas.dart` | upstream edge routing (down/loop-bottom/loop-right); node coordinates are input because Flutter cannot measure pre-layout |
| `flow-expand` | ported | — | `components/flow_expand.dart` | hover expand control and the full-screen zoom viewer shared with the Mermaid element |
| `flow-graph` | ported | — | `components/flow_graph.dart` | grid columns and rows, bezier edges; pending nodes lose the dashed border |
| `follow-up-suggestions` | ported | — | `components/follow_up_suggestions.dart` | chips read from ThreadState.suggestions, edge fades, tap sends the prompt |
| `generative-ui` | ported | — | `components/generative_ui.dart` | component registry with the styled Markdown entry; the library comes from the host, as upstream |
| `guardrail-notice` | ported | — | `components/guardrail_notice.dart` | policy id, explanation and the alternatives to try |
| `heat-graph` | ported | — | `components/heat_graph.dart` | month labels, day labels, legend, per-cell tooltips; the layout lives in components/heat_calendar.dart because upstream delegates it to the heat-graph package |
| `image` | ported | — | `components/image.dart` | loading/ready/failed/blocked frame with hover controls and zoom; download/copy call host callbacks since Dart has no image clipboard |
| `image-generation` | ported | — | `components/image_generation.dart` | pulsing dot field then the gradient reveal; OKLCH gradients converted to sRGB |
| `inline-citation` | ported | — | `components/inline_citation.dart` | numbered chips with a floating source preview; segments replace upstream's hardcoded sentence |
| `job-progress` | ported | — | `components/job_progress.dart` | weighted overall bar, stage names, eta, cancel |
| `launcher-bubble` | ported | — | `components/launcher_bubble.dart` | open/closed bubble with unread badge, prompts and start action |
| `loading-state` | ported | — | `components/indicators.dart` | AssistantLoadingState |
| `logos` | ported | — | `components/logos.dart` | vendor marks drawn from the upstream path data; the arc-based OpenAI mark falls back to a wordmark |
| `map-answer` | ported | — | `components/map_answer.dart` | grid map, pins, hand-dashed route and the pin list |
| `markdown-text` | partial | — | `components/markdown.dart` | fenced code runs through the syntax-highlighter tokenizer and the coldark palettes; ```mermaid fences render through the built-in painters (flowchart, pie, sequence), and inline/display math is typeset by components/math_renderer.dart (fractions, radicals, scripts, environments, the symbol table) with a host renderer overriding it. Constructs outside those subsets still fall back to the styled source |
| `math-block` | ported | — | `components/math_block.dart` | revealed derivation steps plus Frac / Sup / Sub helpers, and display math in markdown falls back to the styled TeX source when the host has no typesetter |
| `mcp-config` | ported | — | `components/mcp_config.dart` | editor (connectors, custom servers, add form, status, auth control) plus the client half of react-mcp in runtime/mcp_client.dart: initialize, tools/list with cursors, tools/call, resources, a merged toolkit per server and per-server failures; http/sse transports, stdio needs a local process |
| `mcp-server-panel` | ported | — | `components/mcp_server_panel.dart` | server rows with status dots, tools and authorize |
| `memory-chips` | ported | — | `components/memory_chips.dart` | fresh vs existing tints, forget control |
| `mermaid-diagram` | ported | — | `components/mermaid_diagram.dart, components/mermaid_renderer.dart, components/mermaid_extra.dart` | frame (skeleton, code fallback, shared zoom surface) plus built-in renderers: graph/flowchart in TD/TB/BT/LR with node shapes, chains and labelled edges; pie with the palette and a legend; sequence with lifelines, arrows, self-messages and notes. All parsed and painted in Dart, so a fence renders with no host engine; class, state and gantt still fall back to the source, and a host drawing overrides the built-in one |
| `message-actions` | ported | — | `components/action_bar.dart` |  |
| `message-attachment` | ported | — | `components/message_attachment.dart` | image and document rows; the runtime wrapper renders through the same list |
| `message-branches` | ported | — | `components/action_bar.dart` |  |
| `message-pair` | ported | — | `components/thread.dart` | AssistantUserMessage + AssistantAssistantMessage |
| `message-queue` | ported | — | `components/message_queue.dart` | stacked queued turns, cancel one by tap |
| `message-timing` | ported | — | `components/message_timing.dart` |  |
| `mobile-composer` | ported | — | `components/mobile_composer.dart` | quick actions, attach, send / stop, keyboard-aware chrome |
| `model-picker` | ported | — | `components/model_picker.dart` | families with capability chips, context and price, current model checked |
| `model-selector` | ported | — | `components/model_selector.dart` | trigger variants and sizes, search over id / name / keywords, disabled rows, reasoning effort with sticky resolution; the compound maps to one widget |
| `number-ticker` | ported | — | `components/number_ticker.dart` | rolling digits on change, grouped figure |
| `onboarding` | ported | — | `components/onboarding.dart` | stepped tour with progress dots, example box and skip / next |
| `permission-grant` | ported | — | `components/permission_grant.dart` | capability, reach list, deny / session / always while pending, then the settled scope |
| `prompt-library` | ported | — | `components/prompt_library.dart` | searchable saved prompts with a preview, variable chips and enter-to-insert |
| `quota-banner` | ported | — | `components/quota_banner.dart` | what is left, amber from 90%, upgrade action |
| `quote-reply` | ported | — | `components/quote_reply.dart` | highlighted selection with the action toolbar and the quoted reply |
| `quote` | ported | — | `components/quote.dart` | quote block, selection toolbar and the composer preview, plus QuotePart in the runtime model so a quoted passage travels as a message part and renders in the bubble |
| `read-aloud` | ported | — | `components/read_aloud.dart` | word-by-word highlight with transport, progress and rate |
| `reasoning` | ported | — | `components/parts.dart` |  |
| `reasoning-effort` | ported | — | `components/reasoning_effort.dart` | segmented levels with the budget spent |
| `reasoning-panel` | ported | — | `components/reasoning_panel.dart` | collapsible steps with a pulsing active dot and the thinking label |
| `recommendation-card` | ported | — | `components/recommendation_card.dart` | confidence bars, accept / alternatives, accepted state |
| `regenerate-menu` | ported | — | `components/regenerate_menu.dart` | model list with the current one marked |
| `research-report` | ported | — | `components/research_report.dart` | section states, per-section source counts and previews |
| `retrieval-chunks` | ported | — | `components/retrieval_chunks.dart` | query pill, passage cards with locator, score and the relevance bar |
| `reviewable-diff` | ported | — | `components/reviewable_diff.dart` | hunks kept or discarded one by one, apply gated on the review |
| `schedule-card` | ported | — | `components/schedule_card.dart` | cadence, next-run block, enable switch, run history |
| `score-breakdown` | ported | — | `components/score_breakdown.dart` | verdict band by ratio, weighted criteria, notes |
| `scroll-anchor` | ported | — | `primitives/thread.dart` | the viewport floats a jump-to-latest pill while unpinned |
| `settings-panel` | ported | — | `components/settings_panel.dart` | model segments, system prompt, temperature slider and the feature switches |
| `shared-conversation` | ported | — | `components/shared_conversation.dart` | read-only turns with continue-in-your-own-chat |
| `shiki-highlighter` | ported | — | `components/syntax_highlighter.dart` | the Shiki-based variant of the highlighter element; this port tokenizes with Dart regexes and paints the same coldark palettes |
| `sources` | ported | — | `components/sources.dart` | collapsible pill with the domain / title cards |
| `speaker-identity` | ported | — | `components/speaker_identity.dart` | per-turn speaker badges tinted by kind |
| `spec-sheet` | ported | — | `components/spec_sheet.dart` | labelled rows with an emphasised value column |
| `stopped-run` | ported | — | `components/action_bar.dart` | a cancelled run keeps its content and offers Continue, which starts a run from that message |
| `streaming-text` | ported | — | `components/streaming_text.dart` | word reveal where each word fades in over 500ms, its fresh tint settles over 700ms and a pulsing caret trails the stream |
| `subagent-list` | ported | — | `components/subagent_list.dart` | per-agent progress bars plus the shimmering summary row |
| `suggestions` | ported | — | `components/suggestions.dart` | pills or list, selected pill inverted |
| `surfaces` | ported | — | `components/surfaces.dart` | the shared paper / field / mono / pill helpers |
| `syntax-highlighter` | ported | — | `components/syntax_highlighter.dart` | Dart tokenizer instead of Prism; the coldark-cold / coldark-dark palettes are taken verbatim from prism-themes, and the markdown fenced blocks color through the same tokens |
| `task-card` | ported | — | `components/task_card.dart` | five states, disclosure transcript, actions and result blocks |
| `terminal-block` | ported | — | `components/terminal_block.dart` | streaming command output with the exit state and caret |
| `thinking-indicator` | ported | — | `components/indicators.dart` | AssistantThinkingIndicator |
| `thread-list` | ported | — | `components/thread_list.dart` | runtime-backed list with search, archive, delete, auto-title |
| `thread-search` | ported | — | `components/thread_search.dart` | pinned first then groups, arrow-key stepping |
| `thread` | ported | — | `components/thread.dart` |  |
| `threadlist-sidebar` | ported | — | `components/threadlist_sidebar.dart` | product header, the thread list and the source link |
| `timeline` | ported | — | `components/timeline.dart` | rail with past / now / future dots and the now ring |
| `todo-list` | ported | — | `components/todo_list.dart` | four states with strikethrough and a failure reason |
| `tool-call` | ported | — | `components/parts.dart` |  |
| `tool-error` | ported | — | `components/tool_error.dart` | name/target/attempt, monospace error block, retry + skip |
| `tool-fallback` | ported | — | `components/parts.dart` |  |
| `tool-group` | ported | — | `components/tool_group.dart` | paper card with counts and per-call rows; the .aui Root/Trigger/Content trio is not ported |
| `tool-timeline` | ported | — | `components/tool_timeline.dart` | swap label, revealed steps and diff stats; every revealed step enters with fade + slide-in-from-bottom over 300ms and the trigger chevron turns on the element curve, both per the upstream classes |
| `tooltip-icon-button` | ported | — | `components/tooltip_icon_button.dart` |  |
| `trace-waterfall` | ported | — | `components/trace_waterfall.dart` | span bars by status with a running pulse |
| `typing-indicator` | ported | — | `components/typing_indicator.dart` |  |
| `voice` | ported | — | `components/composer_voice.dart` | mic toggle + level waveform; streams dictation transcripts |
| `voice-conversation` | ported | — | `components/voice_conversation.dart` | orb scaled by input level, caption per phase and the mute / end transport |
| `web-preview` | partial | — | `components/web_preview.dart, components/web_preview_frame.dart` | the chrome is ported (origin bar, reload, open-in-new, loading state, and it enforces no isolation, matching the element); the frame itself is a platform surface: an iframe registered as a platform view on web, null elsewhere so the host passes its own |
| `web-search` | ported | — | `components/web_search.dart` | query pill, result rows; the read count follows the list instead of upstream's fixed string |

## Totals

| Status | Count |
|---|---|
| ported | 121 |
| partial | 4 |
| planned | 0 |
| n/a | 0 |
| **total** | **125** |
