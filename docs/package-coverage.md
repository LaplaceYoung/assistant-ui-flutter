# Package coverage

Generated by `dart run tool/sync_package_coverage.dart` — do not edit by hand.

| Package | Status | Flutter | Note |
|---|---|---|---|
| `agent-launcher` | ported | `components/launcher_bubble.dart` | the launcher surface; the package's build wiring is React-specific |
| `ai-sdk` | ported | `protocol/data_stream_adapter.dart` | the AI SDK UI message stream is the data stream the adapter reads |
| `assistant-stream` | ported | `protocol/data_stream.dart, protocol/data_stream_adapter.dart` | data stream v1 frames, CRLF-aware and SSE-wrapped, plus the aui-* extensions |
| `cli` | n/a | — | copies React UI source into an app; a Flutter port would ship widgets, not generated source |
| `cloud` | ported | `runtime/cloud.dart` | threads CRUD and thread messages with a token callback; the realtime event stream and files endpoints are not ported |
| `core` | ported | `core/, runtime/local_runtime.dart` | messages, branches, parts, adapters, runtime API; the store and tap responsibilities live in LocalRuntime, which is the port of useLocalRuntime |
| `create-assistant-ui` | n/a | — | scaffolder for React apps |
| `eve` | n/a | — | upstream internal app package |
| `heat-graph` | ported | `components/heat_graph.dart` |  |
| `mcp-docs-server` | n/a | — | docs server for MCP clients |
| `metro` | n/a | — | React Native bundler config |
| `next` | n/a | — | Next.js plugin |
| `react` | ported | `primitives/, components/` | the React package scope is this port whole surface: provider, primitives, hooks and the styled components |
| `react-a2a` | ported | `runtime/a2a.dart` | client, wire types and conversions; push-notification configs and the extended agent card are not ported |
| `react-ag-ui` | ported | `runtime/ag_ui.dart` | event parser, SSE agent and the adapter; the React-side queue and thread-switch orchestration maps onto the Dart runtime |
| `react-ai-sdk` | ported | `protocol/data_stream_adapter.dart` |  |
| `react-data-stream` | ported | `protocol/data_stream_adapter.dart` |  |
| `react-devtools` | n/a | — | browser devtools extension; Flutter has the inspector instead |
| `react-generative-ui` | ported | `runtime/generative_ui.dart` | the IR, the view-source serializer and the host vocabulary; the standard component set is smaller than upstream's full catalogue |
| `react-google-adk` | ported | `runtime/adk.dart` | client, accumulator and conversions; tool confirmations (gated call id, decision projection, the reply that resumes), auth requests, agent transfer and escalation, state/artifact deltas and long-running tool ids |
| `react-hook-form` | n/a | — | React form binding; host concern in Flutter |
| `react-ink` | n/a | — | terminal renderer for React; Flutter ships no terminal target |
| `react-ink-markdown` | n/a | — | markdown for react-ink; same reason |
| `react-langchain` | ported | `runtime/langgraph.dart` | the assistant-id flavour of the same platform API is the LangGraph client |
| `react-langgraph` | ported | `runtime/langgraph.dart` | threads, run streaming, message accumulator and conversions |
| `react-lexical` | n/a | — | Lexical rich-text editor; Flutter has its own editing stack |
| `react-markdown` | ported | `components/markdown.dart` | the block and inline grammar, with fenced code through the syntax highlighter |
| `react-mcp` | ported | `runtime/mcp_client.dart` | client half (initialize, tools, call, toolkit); the panel is components/mcp_config.dart. stdio needs a local process, so http/sse |
| `react-native` | partial | `components/` | the RN element set was used as the layout reference for the Flutter widgets rather than ported file by file |
| `react-o11y` | ported | `runtime/span_tree.dart, components/span_primitives.dart` | the span resource (parents resolved, depths cached, cycles broken, collapse, visible rows, time window) and the primitive family (name, type badge, status, collapse toggle, indent, children, timeline bar); an OTel exporter is a host concern, so spans arrive as SpanData |
| `react-opencode` | n/a | — | code-editor integration for React |
| `react-pi` | n/a | — | React package for the pi runtime; no Dart counterpart yet |
| `react-streamdown` | n/a | — | React streaming markdown renderer; superseded by components/markdown.dart |
| `react-syntax-highlighter` | ported | `components/syntax_highlighter.dart` | the coldark palettes and the tokenizer |
| `safe-content-frame` | n/a | — | sandboxed iframe embed; Flutter has no equivalent surface |
| `store` | ported | `runtime/local_runtime.dart, primitives/state.dart` | thread/threads/composer state with the same slices the React store exposes |
| `svelte` | n/a | — | another framework binding |
| `tap` | ported | `runtime/local_runtime.dart` | run lifecycle, abort, queue and tool execution |
| `tw-shimmer` | ported | `components/shimmer.dart` | the effect with the CSS utility parameters (angle, speed, spread, track, repeat delay) |
| `ui` | ported | `components/, primitives/` | 119 of 125 elements ported, 4 partial, 2 n/a — see docs/element-coverage.md |
| `vite` | n/a | — | Vite plugin |
| `vue` | n/a | — | another framework binding |
| `x-buildutils` | n/a | — | build tooling |
| `x-changelog` | n/a | — | release tooling |
| `x-generative-compiler` | n/a | — | compiler tooling for the React renderer |
| `x-performance` | n/a | — | React performance harness |

## Totals

| Status | Count |
|---|---|
| ported | 23 |
| partial | 1 |
| n/a | 22 |
| **total** | **46** |
